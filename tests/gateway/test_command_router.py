"""
Unit tests for IMAP command classifier.

Every command in the spec table must classify correctly.

Sub-milestone: 0.2
"""
import pytest
from gateway.command_router import classify
from gateway.imap_parser import ParsedCommand


# TODO: add test cases in sub-milestone 0.2
